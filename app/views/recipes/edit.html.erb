<%= form_for @recipe do |f| %>
  <%= f.text_field :name %>
  <%= f.collection_check_boxes(:ingredient_ids, Ingredient.all, :id, :name) do |d|%>
  <%= d.label %>
  <%= d.check_box %>
  <% end %>
  
<%= f.submit 'Update Recipe'%>
<% end %>
